{
	"name": "language-server-open-swift",
	"description": "open-swift implementation of a language server in node.",
	"version": "0.0.1",
	"author": "wsds",
	"license": "MIT",
	"engines": {
		"node": "*"
	},
	"dependencies": {
		"vscode-languageserver": "^2.2.0"
	},
	"devDependencies": {
		"typescript": "^1.8.9"
	},
	"scripts": {
		"compile": "installServerIntoExtension ../client ./package.json ./tsconfig.json && tsc -p .",
		"watch": "installServerIntoExtension ../client ./package.json ./tsconfig.json && tsc --watch -p ."
	}
}
